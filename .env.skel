# Database Configuration
DATABASE_URL=mysql://root:your-secure-password@mysql:3306/your_database_name # container internal port
MYSQL_ROOT_PASSWORD=your-secure-password
MYSQL_DATABASE=your_database_name
MYSQL_PORT=3306 # host mapping

# JWT Secret (REQUIRED - generate with: openssl rand -base64 32)
JWT_SECRET="your-very-long-random-secret-key-change-this"

# Admin User Creation (optional - user is created on first container start if provided)
# If not set, admin user will not be created automatically
# Admin user is only created if it doesn't already exist
ADMIN_USERNAME=your-username
ADMIN_PASSWORD=your-secure-admin-password

# CORS Origin (set to your frontend domain in production)
# Example: https://example.com
# For development, you can leave as * (not recommended for production)
CORS_ORIGIN=*

# Shortener Domain (optional - domain for URL shortener redirects)
# Example: exam.pl
# If not set, redirect functionality will be disabled
SHORTENER_DOMAIN=

# Main Site URL (optional - redirect target when accessing shortener domain root)
# Example: example.com
# If not set and shortener domain root is accessed, shows info message instead of redirect
MAIN_SITE_URL=
